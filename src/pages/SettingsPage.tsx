import { BarChart3, Database, LogOut, MonitorCog, Trash2 } from 'lucide-react'
import { FormEvent, useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import { Button } from '../components/Button'
import { Modal } from '../components/Modal'
import { PageHeader } from '../components/PageHeader'
import { useAppStore } from '../hooks/useAppStore'

export function SettingsPage() {
  const { currentUser, data, updateProfile, updateSettings, logout, clearLocalData } = useAppStore(); const [name, setName] = useState(currentUser?.name ?? ''); const [email, setEmail] = useState(currentUser?.email ?? ''); const [message, setMessage] = useState(''); const [clearOpen, setClearOpen] = useState(false); const navigate = useNavigate()
  function saveProfile(event: FormEvent) { event.preventDefault(); if (!name.trim()) return setMessage('Informe seu nome.'); const result = updateProfile({ name, email }); setMessage(result.ok ? 'Perfil atualizado.' : result.error ?? 'Não foi possível atualizar.') }
  return <div className="animate-fade-in"><PageHeader title="Perfil e configurações" subtitle="Personalize seu espaço e gerencie seus dados." /><form onSubmit={saveProfile} className="rounded-2xl border border-slate-100 bg-white p-4 shadow-sm"><h2 className="section-title">Seus dados</h2><div className="mt-4 space-y-3"><label><span className="field-label">Nome</span><input required value={name} onChange={(event) => setName(event.target.value)} className="field-input" /></label><label><span className="field-label">E-mail</span><input required type="email" value={email} onChange={(event) => setEmail(event.target.value)} className="field-input" /></label>{message && <p role="status" className={`text-xs font-semibold ${message === 'Perfil atualizado.' ? 'text-emerald-600' : 'text-rose-600'}`}>{message}</p>}<Button type="submit" variant="secondary" className="w-full">Salvar alterações</Button></div></form><section className="mt-5 rounded-2xl border border-slate-100 bg-white p-4 shadow-sm"><h2 className="section-title">Preferências</h2><Toggle label="Alertas inteligentes" description="Exibir alertas baseados no seu planejamento." checked={data.settings.notificationsEnabled} onChange={(value) => updateSettings({ notificationsEnabled: value })} /><Toggle label="Interface compacta" description="Reduz os espaçamentos em listas e cards." checked={data.settings.compactMode} onChange={(value) => updateSettings({ compactMode: value })} /></section><section className="mt-5 space-y-2"><Link to="/estatisticas" className="flex items-center justify-between rounded-2xl border border-slate-100 bg-white p-4 shadow-sm"><span className="flex items-center gap-3"><span className="rounded-xl bg-brand-50 p-2 text-brand-700"><BarChart3 size={18} /></span><span><span className="block text-sm font-bold text-slate-700">Estatísticas</span><span className="text-xs text-slate-500">Acompanhe sua evolução</span></span></span><span className="text-brand-600">›</span></Link><button onClick={() => setClearOpen(true)} className="flex w-full items-center gap-3 rounded-2xl border border-rose-100 bg-rose-50 p-4 text-left"><span className="rounded-xl bg-white p-2 text-rose-600"><Database size={18} /></span><span><span className="block text-sm font-bold text-rose-700">Limpar dados locais</span><span className="text-xs text-rose-600">Remove contas, atividades e projetos deste dispositivo</span></span></button><button onClick={() => { logout(); navigate('/boas-vindas') }} className="flex w-full items-center gap-3 rounded-2xl px-4 py-3 text-left text-slate-500 hover:bg-slate-50"><LogOut size={18} /><span className="text-sm font-bold">Sair da conta</span></button></section>{clearOpen && <Modal title="Limpar dados locais?" onClose={() => setClearOpen(false)}><p className="text-sm leading-6 text-slate-600">Isso apagará permanentemente todas as contas, atividades, projetos, etapas e preferências salvos neste dispositivo.</p><div className="mt-5 flex gap-3"><Button variant="secondary" onClick={() => setClearOpen(false)} className="flex-1">Cancelar</Button><Button variant="danger" onClick={() => { clearLocalData(); navigate('/boas-vindas') }} className="flex-1">Limpar dados</Button></div></Modal>}</div>
}
function Toggle({ label, description, checked, onChange }: { label: string; description: string; checked: boolean; onChange: (value: boolean) => void }) { return <label className="mt-4 flex cursor-pointer items-center justify-between gap-3"><span><span className="block text-sm font-bold text-slate-700">{label}</span><span className="mt-0.5 block text-xs leading-5 text-slate-500">{description}</span></span><input type="checkbox" className="peer sr-only" checked={checked} onChange={(event) => onChange(event.target.checked)} /><span className="relative h-6 w-11 shrink-0 rounded-full bg-slate-200 transition peer-checked:bg-brand-600 after:absolute after:left-1 after:top-1 after:h-4 after:w-4 after:rounded-full after:bg-white after:transition peer-checked:after:translate-x-5" /></label> }
